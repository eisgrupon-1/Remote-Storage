Class {
	#name : #FileManagementSystemTests,
	#superclass : #TestCase,
	#category : #'RemoteStorage-Tests'
}

{ #category : #tests }
FileManagementSystemTests >> testDefault [
	"Se agrega temporalmente para que funcione CI"

	self assert: true
]
